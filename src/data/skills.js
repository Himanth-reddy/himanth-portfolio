import { siteContent } from './siteContent.js'

export const marqueeItems = siteContent.marquee.items

export const aboutContent = {
  headingMain: siteContent.about.headingMain,
  headingAccent: siteContent.about.headingAccent,
  headingSuffix: siteContent.about.headingSuffix,
  paragraphs: siteContent.about.paragraphs,
  nowText: siteContent.about.nowText,
}

export const skillGroups = siteContent.about.skillGroups
